services:
  - type: web
    name: veille-api
    runtime: python
    buildCommand: ""
    startCommand: uvicorn api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
